import { PasswordTemplateFormValidationDirective } from './password-template-form-validation.directive';

describe('PasswordTemplateFormValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordTemplateFormValidationDirective();
    expect(directive).toBeTruthy();
  });
});
